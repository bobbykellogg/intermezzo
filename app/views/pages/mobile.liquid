---
title: Mobile

# true if the page is included in the menu
listed: false
# true if the page is published
published: true

# true if the page can be used as a layout for new pages created by the editors
# is_layout: true

# position among sibling pages
# position: 1

# sets a redirection to the given url (301)
# redirect_url: "<url to a page or to a remote url>"

# content type that this page is templatizing
# content_type: "<slug of one of the content types>"

# editable_elements:
#   'some_block/some_slug': "<text>"
#   'some_block/some_slug': "<relative path to the file under the public/samples folder>"
---
<!doctype html>
<html>
  <head lang="en">
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>Intermezzo Bar + Caf&eacute; by American Riviera Dining</title>
    
    <meta name="description" content="{{ site.meta_description }}">
    <meta name="keywords" content="{{ site.meta_keywords }}">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <link rel="stylesheet" href="{{ 'mobile.css' | stylesheet_url }}">
  </head>
  <body>
    <div id="page-mobile">
      <header id="mobile-header">
        <a href="/" id="logo">{{ 'splash_logo_intermezzo.png' | theme_image_tag: alt:site.name }}"></a>

        <h1>Intermezzo Bar + Cafe</h1>

        <ul>
          <li><a href="tel:+18059669463">(805) 966-9463</a></li>
          <li><a href="http://maps.google.com?q=813 Anacapa Street, Santa Barbara, CA 93101">813 Anacapa Street, Santa Barbara, CA 93101</a></li>
        </ul>

        {% block header %}
          {% editable_long_text 'header' %}{% endeditable_long_text %}
        {% endblock %}

        <p><a href="/" class="button" id="full-site-button">View full site</a></p>
      </header>

      {% block content %}
        {% editable_long_text 'content' %}{% endeditable_long_text %}
      {% endblock %}

      <footer id="mobile-footer">
        <dl>
          <dt>American Riviera Dining Sites:</dt>
          <dd><a href="/">Intermezzo</a></dd>
          <dd><a href="http://winecask.com" rel="external">Wine Cask</a></dd>
          <dd><a href="http://bouchon.com">Bouchon</a></dd>
        </dl>
        
        <p id="copyright">&copy;<?php print date('Y'); ?> Intermezzo Bar + Café / Website by <a href="http://cmnd-o.com" rel="external">CMND-O</a></p>
      </footer>
    </div>
  </body>

  {{ 'jquery.js'  | javascript_tag }}
  {{ 'plugins.js' | javascript_tag }}
  {{ 'mobile.js'  | javascript_tag }}
</html>
